name: Scraper Personas

permissions:
  contents: write

on:
  schedule:
    - cron: "15 5 12 * *"
  workflow_dispatch:

jobs:
  personas:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run scraper Personas
        run: |
          python scrapers/scraper_personas_desaparecidas.py

      - name: Commit data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/personas_desaparecidas
          git commit -m "chore: update personas desaparecidas" || echo "No changes"
          git pull --rebase origin main
          git push
